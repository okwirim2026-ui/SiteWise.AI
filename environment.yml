name: sitewise-ai
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - ultralytics
    - segment-anything
    - opencv-python
    - matplotlib
    - numpy
    - pillow
